getwd()
rm( list= ls())
# we start by loading libraries
require( MASS )
require( car )
require( reshape )
require( ggplot2 )
require( plyr )
require( xtable )
require( vcd )
# I'm going to read in some functions up here before we start:
# source( "./WRP_FUNCTIONS.R" )
setwd( "../data" )
